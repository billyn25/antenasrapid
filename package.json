{
  "name": "antenasrapid",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node --test tests/*.test.mjs && node scripts/build.mjs && node scripts/audit.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview": "node scripts/serve.mjs"
  }
}
